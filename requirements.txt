pypdf>=3.0.0
PyQt6>=6.4.0
PyMuPDF>=1.23.0
Pillow>=10.0.0
pdf2docx>=0.5.6
docx2pdf>=0.1.8
# Build tools (not needed at runtime — install separately for building)
# nuitka>=2.0
# ordered-set>=4.1
# zstandard>=0.22
